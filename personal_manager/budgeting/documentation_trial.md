# Trial 1

Plan is to run a simple expense entry from command line and this will run everytime system has started and after 10:00 pm. 

The data is stored in a csv file which will enter as a new line with timestamp.

I have also added a `run_daily` function which can trigger the function at a specific time daily.


# Issue 1
The `trial.py` creates a csv file but it doesn't recognize the issue if the file is there with no headings. NEED TO RECTIFY THIS.

# Trial 2
