# Idea

## First step is to make the app as locally installable.

## Second step is to create password features and then the financial helper


I want, in budgeting, to take my info about expenses and the amount spent and store it in a database.



## Make the app in steps, for each function.

## When creating each function (each function is in a separate folder), create a documentation_trial markdown file to document what is being built. Also create an __init__.py file to make each function a module.


### Make everything work as a prototype, then build up incremently.